services:
  db:
    image: postgres:15
    container_name: java_framework_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ecole_db
    ports:
      - "5432:5432"